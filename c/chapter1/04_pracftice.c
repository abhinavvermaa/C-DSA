#include<stdio.h>

int main(){
    int principle=100, rate = 2, years=2; 
    int simpleinterest = (principle *rate *years)/100;
    printf("the simple interest is %d",simpleinterest);

    return 0;

}